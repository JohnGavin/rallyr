% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_swingvision.R
\name{parse_swingvision}
\alias{parse_swingvision}
\title{Parse pasted SwingVision "Rally" session text into a tidy data frame}
\usage{
parse_swingvision(text, captured_on = Sys.Date(), me)
}
\arguments{
\item{text}{Character scalar. The pasted text. Sessions may be separated by
lines of \verb{===}; a leading session before the first separator is also read.}

\item{captured_on}{A \code{Date} giving the day the text was copied from the web
page. Used to resolve relative dates (\code{"Monday"}, \code{"Yesterday"}, \code{"Today"})
to absolute dates. Defaults to today.}

\item{me}{The player name (as it appears in the pasted text) to treat as
"you", i.e. the \verb{me_*} columns below. Required if \code{text} contains any
session blocks; ignored for empty input.}
}
\value{
A tibble, one row per unique match, ordered by \code{played_at}.
}
\description{
Parses the plain text copied from a SwingVision match/rally page (the
rendered stats, not the URL) into one row per match. Handles the app's
quirks: value-before-label layout, duplicated \verb{Cross-Court In} labels under
both Forehands and Backhands (resolved by tracking the current section),
relative dates such as \code{"Monday"}, and mixed distance units (mi / ft).
}
\details{
Multiple pasted views of the same match (e.g. the simple "Groundstrokes"
view and the "Advanced" Forehands/Backhands view) share a \code{played_at} key
and are coalesced into a single complete row. Exact duplicates collapse.
}
\section{Data-quality caveats}{

\code{longest_rally} is unreliable: values are implausibly long for a single
rally and almost certainly reflect SwingVision merging several consecutive
rallies into one (it does not reliably split points during continuous
hitting practice). Kept as recorded but do not treat as a true rally length.
\code{shots_per_hour} is labelled as a rate by the app but its magnitude tracks a
shots-in count. Prefer \code{me_shots}, \code{shots_in_pct}, and the groundstroke
accuracy fields for real signal.
}

