% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{tennis_db_upsert}
\alias{tennis_db_upsert}
\title{Upsert rows into a database table (idempotent)}
\usage{
tennis_db_upsert(con, matches, key = "match_id", table = "matches")
}
\arguments{
\item{con}{A DBI connection from \code{\link[=tennis_db_connect]{tennis_db_connect()}}.}

\item{matches}{A tibble from \code{\link[=fetch_swingvision]{fetch_swingvision()}} (or \code{\link[=parse_swingvision]{parse_swingvision()}}
with \code{key = "played_at"}), or any other tibble to upsert into \code{table}.}

\item{key}{Name of the key column. Defaults to \code{"match_id"}.}

\item{table}{Name of the destination table. Defaults to \code{"matches"}.}
}
\value{
Invisibly, the number of rows written.
}
\description{
Writes rows to \code{table} (default \code{"matches"}). Rows are keyed on \code{key}
(default \code{match_id}, the SwingVision match id from \code{\link[=fetch_swingvision]{fetch_swingvision()}}):
any existing rows with a matching key are deleted and replaced, so
re-fetching a previous week is a no-op (or an in-place correction). A
one-to-many key (several rows sharing one \code{key} value, e.g. several
segments per \code{match_id}) works the same way — every existing row for that
key is deleted, then all of the new rows are appended.
}
