% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{tennis_weather}
\alias{tennis_weather}
\title{Read the session_weather table back as a tibble}
\usage{
tennis_weather(con)
}
\arguments{
\item{con}{A DBI connection from \code{\link[=tennis_db_connect]{tennis_db_connect()}}.}
}
\value{
A tibble of all stored session weather, ordered by \code{match_id}.
Empty (zero-row, correctly-typed) if the table doesn't exist yet.
}
\description{
One row per match, as written by \code{scripts/update_weather.R} from the
Open-Meteo historical-weather API (see \code{\link[=fetch_weather]{fetch_weather()}},
\code{\link[=sv_session_weather]{sv_session_weather()}}): ambient \code{temp_c}, \code{wind_avg_kmh} and
\code{wind_gust_kmh} for the hour
nearest that session's start time, at that session's location
(\code{matches$latitude}/\code{longitude}). Fetched for every session regardless of
indoor/outdoor — see \code{R/weather.R}'s header for why.
}
