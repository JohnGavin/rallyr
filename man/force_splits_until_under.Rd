% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_quality_params.R
\name{force_splits_until_under}
\alias{force_splits_until_under}
\title{Propose splits for rallies flagged for an excess stroke count}
\usage{
force_splits_until_under(gaps, n, max_strokes, seed = integer(0))
}
\arguments{
\item{gaps}{Numeric gaps between consecutive strokes; \code{gaps[i]} is the
pause between stroke \code{i} and \code{i + 1}, so a split "after \code{i}" uses index
\code{i}. Length \code{n - 1}.}

\item{n}{Number of strokes in the rally.}

\item{max_strokes}{Threshold above which a fragment must be split again.}

\item{seed}{Split indices already decided by the gap-threshold pass. They
are kept and only topped up. Passing them matters: a gap-threshold split
can itself leave a fragment over the threshold (observed on the 8 Jun
rally 42 -- 51 strokes cut into 8 + 43, with 43 still over 40), and
before this the forced pass ran only when the gap pass found \emph{nothing},
so that fragment was reported "still over threshold" and left alone.}

\item{rallies_flagged}{Output of \code{\link[=dq_flag_rallies]{dq_flag_rallies()}} — must include
\code{match_id}, \code{rally_id}, \code{regime}, \code{n_shots}, \code{flag_reason}.}

\item{shot_detail}{A tibble with (at least) \code{match_id}, \code{rally_id},
\code{started_at} — see \code{tennis_shot_detail()} in \code{R/db.R}.}

\item{params}{A \code{dq_params()}-shaped list; pass a custom one to explore a
different threshold.}
}
\value{
A tibble, one row per proposed sub-rally: \code{match_id} (rally IDs
are only unique WITHIN a match — several flagged rallies can share the
same numeric \code{rally_id} across different matches, so \code{match_id} is
required to disambiguate), \code{orig_rally_id}, \code{orig_n_shots},
\code{new_rally_id}, \code{new_n_shots}, \code{split} (logical — was this rally
actually split), \code{split_method} (\code{"none"} / \code{"gap_threshold"} /
\code{"forced_largest_gap"}), \code{still_flagged} (logical — does the new
sub-rally still exceed its own regime's stroke threshold).

An increasing integer vector of split indices, possibly empty.
}
\description{
For every rally in \code{rallies_flagged} currently flagged for exceeding its
stroke-count threshold (not the speed threshold — a fast rally isn't a
grouping-boundary problem), looks up its shots in \code{shot_detail} and
proposes a split in \strong{two passes}:
}
\details{
\enumerate{
\item \strong{Gap-threshold pass} — \code{\link[=find_rally_splits]{find_rally_splits()}} against the
calibrated \code{params$max_stroke_gap_sec} (a genuine, independently
plausible pause). Today's data has none (see \code{dq_params()}'s docs).
\item \strong{Forced-largest-gap pass} (2026-09-01, issue #14 reopened) —
for any rally STILL over its stroke threshold after pass 1 finds
nothing, split at the single largest internal gap regardless of
whether that gap alone clears the absolute threshold. A rally that
violates \code{max_rally_strokes} represents more than one real point by
definition; pass 1 alone was silently leaving it unresolved on the
Exceptions page ("no qualifying gap — unsplit" for every one of the
14 flagged rallies) instead of proposing the best available split
point. This is a single split, not recursive re-splitting — a
sub-rally that's still over threshold afterwards is reported as such
(\code{still_flagged}), not force-resolved further.
}

This is a \strong{proposed} correction for human review (the Exceptions
page's before/after table) — it does NOT write anything back to
\code{tennis.duckdb}, and does not feed any other chart. A rally with no
internal gap at all (a single-shot artifact — \code{nrow(shots) < 2}) passes
through unsplit, not silently dropped from the output.
}
\keyword{internal}
