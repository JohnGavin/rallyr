% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{tennis_rallies}
\alias{tennis_rallies}
\title{Read the rallies table back as a tibble}
\usage{
tennis_rallies(con)
}
\arguments{
\item{con}{A DBI connection from \code{\link[=tennis_db_connect]{tennis_db_connect()}}.}
}
\value{
A tibble of all stored rallies, ordered by \code{match_id}, \code{rally_id}.
Empty (zero-row, correctly-typed) if the table doesn't exist yet.
}
\description{
One row per rally (point), as written by \code{scripts/update_segments.R} from
the shots' own \code{rally_id} (SwingVision's \code{pid} field — see
\code{\link[=sv_parse_shots]{sv_parse_shots()}}): \code{n_shots}, \code{duration_sec}, and the \code{regime} the
rally sits within (the mode of its shots' regimes).
}
