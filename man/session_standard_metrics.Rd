% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standard_metrics.R
\name{session_standard_metrics}
\alias{session_standard_metrics}
\title{Session performance metrics on one comparable scale}
\usage{
session_standard_metrics(m)
}
\arguments{
\item{m}{Merged per-session tibble (\code{\link[=merge_same_day_sessions]{merge_same_day_sessions()}} output),
ordered chronologically (oldest first), with at least 2 rows.}
}
\value{
A long tibble, one row per session-metric: \code{session_date},
\code{metric} (a short stable key), \code{label} (display text), \code{unit}, \code{value}
(the length-normalised value), \code{direction} (\code{1}/\code{-1}/\code{NA}) and \code{pct}
(0-100 percentile of \code{value} within that metric across sessions).
}
\description{
ISSUES.md #32: the tracked session metrics are raw totals in incompatible
units (strokes, miles, mph, minutes), so a longer session scores higher on
most of them for no reason other than being longer. This function fixes the
unit problem in two steps, deliberately kept separate because they answer
different questions:
}
\details{
\enumerate{
\item \strong{Length-normalisation.} Every metric is defined as a percentage or a
per-minute rate, never a raw total, so a 36-minute session and a
58-minute one are directly comparable. This is the "standardised"
the issue asks for.
\item \strong{Cross-metric comparability.} A percentage and a km/h still cannot be
ranked against each other, so each metric is additionally expressed as
its percentile within its \emph{own} distribution across all sessions. That
is what makes "which metric did this session stand out on?" answerable,
and it is what \code{\link[=session_metric_ranks]{session_metric_ranks()}} ranks.
}

This is deliberately NOT \code{\link[=session_effort_metrics]{session_effort_metrics()}}, which z-scores four
raw metrics and averages them into one composite index. That answers "how
hard was this session overall"; this answers "which aspects of this session
were strong or weak relative to my own history", and keeps the metrics
separate rather than collapsing them.

\strong{Direction.} Ranking implies a better/worse axis, and not every metric has
one. \code{direction} is \code{1} where higher is better, \code{-1} where lower is better,
and \code{NA} for descriptive metrics with no defensible direction — forehand
share is the case in point: an unusually forehand-heavy session is a fact
about the drill (ISSUES.md #3), not an achievement. \code{NA}-direction metrics
are returned, so they can still be read and plotted, but \code{\link[=session_metric_ranks]{session_metric_ranks()}}
excludes them from the ranking rather than silently implying they are good.
}
\seealso{
\code{\link[=session_metric_ranks]{session_metric_ranks()}} for the per-session ranking the bump
chart plots, and \code{\link[=session_effort_metrics]{session_effort_metrics()}} for the composite index.
}
