% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_quality_params.R
\name{dq_stroke_gaps}
\alias{dq_stroke_gaps}
\title{Inter-stroke gap times within every rally}
\usage{
dq_stroke_gaps(shot_detail)
}
\arguments{
\item{shot_detail}{A tibble with (at least) \code{match_id}, \code{rally_id},
\code{started_at} — see \code{tennis_shot_detail()} in \code{R/db.R}.}
}
\value{
A tibble: \code{match_id}, \code{rally_id}, \code{gap_sec}.
}
\description{
The full distribution \code{dq_params()}'s \code{max_stroke_gap_sec} threshold was
calibrated against — one row per consecutive within-rally pause, so a
rally of \code{n} strokes contributes \code{n - 1} rows. Used both to justify the
threshold (see \code{dq_params()}'s docs) and to draw the calibration chart on
the artifact's Exceptions page.
}
