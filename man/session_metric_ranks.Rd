% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standard_metrics.R
\name{session_metric_ranks}
\alias{session_metric_ranks}
\title{Rank each session's metrics against each other}
\usage{
session_metric_ranks(sm)
}
\arguments{
\item{sm}{Output of \code{\link[=session_standard_metrics]{session_standard_metrics()}}.}
}
\value{
A tibble with \code{session_date}, \code{metric}, \code{label}, \code{pct}, \code{score}
(direction-adjusted percentile) and \code{rank} (1 = best that session).
}
\description{
The input for the bump chart (ISSUES.md #32): within each session, the
directional metrics from \code{\link[=session_standard_metrics]{session_standard_metrics()}} are ranked by their
percentile, so rank 1 is the aspect that session was strongest on relative
to the player's own history. Lines joining one metric's rank across
consecutive sessions are what the bump chart draws.
}
\details{
Metrics with \code{direction = NA} are dropped, not ranked — see
\code{\link[=session_standard_metrics]{session_standard_metrics()}} for why. A metric where lower is better has
its percentile flipped before ranking, so rank 1 always means "best".
}
