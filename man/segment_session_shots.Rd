% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_shots.R
\name{segment_session_shots}
\alias{segment_session_shots}
\title{Segment a session's per-shot data into structural drill phases}
\usage{
segment_session_shots(
  shots,
  guest_right_handed = TRUE,
  bin_minutes = 1,
  min_shots_per_bin = 4,
  volley_share_threshold = 0.4,
  mini_tennis_speed_mph = 35,
  wing_dominance_threshold = 0.7
)
}
\arguments{
\item{shots}{A tibble from \code{\link[=sv_parse_shots]{sv_parse_shots()}} (one match's shots).}

\item{guest_right_handed}{Is the "guest" player ("me") right-handed?
Determines which wing maps to forehand vs backhand. Default \code{TRUE} --
the only case seen in this project's data so far.}

\item{bin_minutes}{Width of the time bin used for regime detection.
Default \code{1}.}

\item{min_shots_per_bin}{Minimum total shots in a bin to classify it;
bins below this are \code{"gap"} (a real break in play, not forced into a
guess). Default \code{4}.}

\item{volley_share_threshold}{Host volley-share at/above which a bin is
\code{"net_volley"}. Default \code{0.4}.}

\item{mini_tennis_speed_mph}{Guest ground-stroke mean speed below which
a (non-volley) bin is \code{"mini_tennis"}. Default \code{35}.}

\item{wing_dominance_threshold}{Guest ground-stroke right-wing share
at/above (or at/below \code{1 - threshold}) which a bin is a cross-court
phase. Default \code{0.7}.}
}
\value{
A tibble, one row per detected segment (contiguous bins sharing a
regime), ordered by \code{start_time}: \code{segment_index}, \code{regime}
(\code{"mini_tennis"}/\code{"net_volley"}/\code{"forehand_cross_court"}/
\code{"backhand_cross_court"}/\code{"mixed"}/\code{"gap"}), \code{start_time}, \code{end_time},
\code{duration_min}, \code{n_shots}, \code{n_host_shots}, \code{n_guest_shots}. \code{"gap"} rows
are real breaks in play (a real segment boundary can fragment into
segment/gap/segment) -- callers should treat \code{"gap"} as filler, not a
drill phase.
}
\description{
Detects drill phases from three independent per-shot signals -- host's
volley share (net-volley phases), guest ground-stroke speed (a slower
warm-up phase), and guest's hit_wing dominance (forehand vs backhand
cross-court). Every threshold is a function argument, so this is a
generic bin classifier -- but its \emph{defaults} were calibrated against one
specific drill routine (mini-tennis warmup, opponent-at-net volleying,
forehand cross-court, backhand cross-court, opponent-at-net volleying
again), verified against one real session (mini-tennis ground-strokes
averaged ~12 m/s vs ~20-23 m/s everywhere else; the two volley phases had
host volley shares of 60-85\% vs 0\% elsewhere; the two cross-court phases
showed >85\% wing dominance in opposite directions). Treat the default
thresholds as a worked example, not a universal calibration -- recompute
them for a drill routine that looks different from this one.
}
