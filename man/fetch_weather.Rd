% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weather.R
\name{fetch_weather}
\alias{fetch_weather}
\title{Fetch one day's hourly temperature + wind for a session's location}
\usage{
fetch_weather(lat, lon, date, tz = "Europe/London")
}
\arguments{
\item{lat, lon}{Session location (\code{matches$latitude}/\code{longitude}).}

\item{date}{A \code{Date} (or "YYYY-MM-DD" string) — \code{matches$session_date}.}

\item{tz}{IANA timezone for the hourly series, so "hour 13" means local
13:00 at the venue, not UTC. Defaults to \code{"Europe/London"} — this
project's only location so far.}
}
\value{
The parsed JSON payload (\verb{$hourly$time}, \verb{$hourly$temperature_2m},
\verb{$hourly$wind_speed_10m}, \verb{$hourly$wind_gusts_10m}) — see
\code{\link[=weather_parse]{weather_parse()}} to reduce it to one row. Gust speed matters as much as
average for tennis (a strong gust disrupts a single shot; the average
doesn't capture that), so both are fetched.
}
\description{
Every session's \code{latitude}/\code{longitude} is already captured by
\code{\link[=sv_parse_match]{sv_parse_match()}}; this looks up the ambient weather at that location on
that date. Fetched for every session regardless of indoor/outdoor — wind
is only meaningful outdoors, but temperature is a reasonable proxy for
ambient/exertion conditions either way (issue #3's fatigue
investigation: is the 1st-vs-2nd net-volley rally-length drop actually a
temperature confound?).
}
