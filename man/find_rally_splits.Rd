% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rally_split.R
\name{find_rally_splits}
\alias{find_rally_splits}
\title{Find within-rally split points from stroke timing gaps}
\usage{
find_rally_splits(started_at, ended_at = NULL, max_gap_sec)
}
\arguments{
\item{started_at}{Ordered (ascending) vector of stroke start timestamps
(\code{POSIXct}, \code{Date}, or numeric seconds) for one rally.}

\item{ended_at}{Ordered vector of stroke end timestamps, same length as
\code{started_at}. Optional — when \code{NULL} (default), the gap is computed
between consecutive \code{started_at} values instead. This default exists
because, as of this writing, the persisted \code{shot_detail} table (see
\code{R/db.R}'s \code{tennis_shot_detail()}) stores only \code{started_at} per shot —
there is no \code{ended_at} column in the real schema. Pass \code{ended_at}
explicitly only once/if a stroke-end timestamp becomes available; doing
so gives a more accurate gap (excludes the stroke's own swing duration
from the pause) and is exercised by this function's own tests, but is
not required for it to work against the current schema.}

\item{max_gap_sec}{Numeric threshold in seconds. A gap strictly greater
than this is treated as a likely rally boundary. Not derived by this
function — pass the project's calibrated threshold (see
issue #5).}
}
\value{
Integer vector of split-point indices \code{i}, each satisfying
\verb{1 <= i < length(started_at)}, meaning "a split occurs between stroke
\code{i} and stroke \code{i + 1}". A rally with \code{k} qualifying gaps splits into
\code{k + 1} sub-rallies. Empty (\code{integer(0)}) when no gap exceeds the
threshold, or when fewer than 2 strokes are supplied.
}
\description{
A rally's shots are grouped by the SwingVision API's own \code{pid} field,
taken as-is with no time-gap heuristic (see issue #3). This can
silently merge two or more real rallies into one \code{pid} run when play
actually stopped and restarted between them, producing an implausible
stroke count for a single continuous point (see issue #5, point 4:
"the actual re-grouping/splitting algorithm for a flagged over-long
rally... Not yet solved").
}
\details{
This is a \strong{pure, dependency-free} function: given ordered per-stroke
timestamps for ONE rally, it finds the point(s) where the pause between
one stroke and the next is implausibly large for a live point — i.e.
where a split is more likely than a genuine single rally. It performs NO
database access and makes NO empirical threshold decision itself; the
caller supplies \code{max_gap_sec}.
}
