% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{read_indoor_overrides}
\alias{read_indoor_overrides}
\title{Read the manual indoor/outdoor override}
\usage{
read_indoor_overrides(path = "data-raw/indoor_overrides.txt")
}
\arguments{
\item{path}{Path to the override file, \verb{session_date,TRUE|FALSE} per line.
Defaults to \code{data-raw/indoor_overrides.txt}, resolved relative to the
current working directory (repo root — callers \code{setwd()} there first,
matching \code{\link[=tennis_db_connect]{tennis_db_connect()}} and \code{scripts/update_db.R}).}
}
\value{
A named logical vector, keyed by \code{session_date} formatted
\code{"YYYY-MM-DD"}.
}
\description{
The SwingVision API's \code{court.outdoor} field is a single venue-level
profile, not a per-session value, and is wrong for most sessions at a
venue with both indoor and outdoor courts — see \code{ISSUES.md} #2. Until a
reliable per-session API field exists, indoor/outdoor is recorded here by
hand (verified per session by inspecting that session's video thumbnail),
as the single source of truth for every consumer (currently
\code{scripts/make_artifact_charts.R}; \code{dashboard.qmd} may adopt it too).
}
