% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weather.R
\name{sv_session_weather}
\alias{sv_session_weather}
\title{Fetch + parse weather for one session, in one call}
\usage{
sv_session_weather(lat, lon, date, start_time, tz = "Europe/London")
}
\arguments{
\item{lat, lon}{Session location (\code{matches$latitude}/\code{longitude}).}

\item{date}{A \code{Date} (or "YYYY-MM-DD" string) — \code{matches$session_date}.}

\item{start_time}{A \code{"HH:MM"} string (\code{matches$start_time}).}

\item{tz}{IANA timezone for the hourly series, so "hour 13" means local
13:00 at the venue, not UTC. Defaults to \code{"Europe/London"} — this
project's only location so far.}
}
\value{
A one-row tibble: \code{temp_c}, \code{wind_avg_kmh}, \code{wind_gust_kmh} (see
\code{\link[=weather_parse]{weather_parse()}}).
}
\description{
Fetch + parse weather for one session, in one call
}
